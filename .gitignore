# Rust
/target
/target-linux
/target-rpm
# Cargo.lock is committed: wusel is a binary (reproducible builds).

# RPM build output
/dist

# mise
.mise.local.toml

# Antora build output
documentation/build/

# Editor/OS
.DS_Store
*.swp
*~
.idea/
.vscode/

# Merge/patch leftovers
*.orig
*.rej

# Claude Code working dirs: agent worktrees and machine-local settings
# (settings.local.json). Nothing under .claude/ is part of the project — ignore
# the whole directory, not just the worktrees, so a stray local file cannot be
# committed by accident.
.claude/

# Native Nautilus extension build output
integration/nautilus/*.so
